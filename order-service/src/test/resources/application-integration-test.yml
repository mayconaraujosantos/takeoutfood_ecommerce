spring:
  cloud:
    config:
      enabled: false
  config:
    import: "optional:configserver:"
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

eureka:
  client:
    enabled: false

management:
  tracing:
    enabled: false
