nameOverride: notification-service

image:
  repository: localhost/ifood-clone/notification-service
  tag: local

service:
  port: 8087
  targetPort: 8087

env:
  MONGODB_URI: mongodb://ifood_user:ifood_pass@mongodb:27017/ifood_notifications
  KAFKA_BOOTSTRAP_SERVERS: kafka:9092
